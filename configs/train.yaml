seed: 42
model_dir: ../../models/fnet-tts
resume_checkpoint: false

train:
  fp16: true
  batch_size: 32
  num_epochs: 1000
  save_interval: 100

data:
  data_dir: ./DATA

model:
  n_mel: 80

  embedding:
    n_phoneme: 118
    n_accent: 19

  encoder:
    channels: 192
    n_layers: 12
    dropout: 0.1

  variance_adopter:
    channels: 192
    dropout: 0.1

  decoder:
    channels: 192
    n_layers: 12
    kernel_size: 7
    dropout: 0.1

optimizer:
  lr: 1.0
  betas: [0.9, 0.98]
